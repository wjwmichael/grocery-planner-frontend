import { Component } from 'react';
import { Button } from 'antd';
const Content = ({})=>{
    return (
        <Button size="large" ghost>Start Planning Now </Button>
    );
}

export default Content;

